# 2026-08-15 E3 Hive MR

## Scope

Validate representative Hive 4 operations on MapReduce with the warehouse and test data on CephFS.

## Environment

- Environment ID: E3
- Commit: `095754c`
- Component versions: Hive 4.0.1; Hadoop 3.3.6; OpenJDK 11; E2 CephFS backend

## Commands

`UNKNOWN`: the exact Beeline invocation is not present in tracked evidence. The executed SQL is tracked in
[`scripts/eco/e3-hive-suite.sql`](../../scripts/eco/e3-hive-suite.sql).

## Results

| Test ID | Result | Evidence |
|---|---|---|
| ECO-HIVE-02 | PASS | `application_1786771004480_0019`–`application_1786771004480_0028`; Text, ORC, and Parquet data were read with expected aggregates |
| ECO-HIVE-03 | PARTIAL | Insert/overwrite paths completed without staging residue; the full case matrix was not run |
| ECO-HIVE-04 | PARTIAL | Dynamic partition insertion worked for three partitions, below the case's scale requirement |
| ECO-HIVE-06 | PASS | `ANALYZE TABLE` completed for the tested data |
| ECO-HIVE-07 | PASS | `MSCK REPAIR` discovered the prepared partitions |
| ECO-HIVE-09 | PARTIAL | Truncate behavior completed for the purge-external test table, not a full managed-table matrix |
| ECO-HIVE-10 | PASS | Parquet CTAS completed and read back correctly |
| ECO-HIVE-14 | PARTIAL | MapReduce engine path passed; Tez evidence is in a separate report |

## Findings

Hive required user-classpath-first behavior to prevent Hadoop's older protobuf from shadowing Hive's
protobuf dependency. The tested tables were purge external tables under Hive 4 strict managed-table
rules, so the results do not establish ACID support.

## Limitations

ACID, authorization, large partition counts, Trash behavior, and multi-user identity semantics were
not verified. Client-session residue remained an open issue.

## Follow-up

Retain Hive status as partial until the outstanding ECO-HIVE cases have dated evidence.
