# 2026-08-15 E3 Hive Tez

## Scope

Validate the representative Hive SQL suite using Tez and observe native client-session lifecycle.

## Environment

- Environment ID: E3
- Commit: `34748f7`
- Component versions: Hive 4.0.1; Tez 0.10.4; Hadoop 3.3.6; OpenJDK 11; E2 CephFS backend
- Tez runtime archive: package `share/tez.tar.gz`, 77,713,127 bytes, distributed through HDFS

## Commands

`UNKNOWN`: the exact Beeline invocation is not present in tracked evidence. The executed SQL is tracked in
[`scripts/eco/e3-hive-tez-suite.sql`](../../scripts/eco/e3-hive-tez-suite.sql).

## Results

| Test ID | Result | Evidence |
|---|---|---|
| ECO-HIVE-14 | PASS | `application_1786771004480_0033`; six submitted Tez DAGs completed successfully for the representative SQL suite |
| ECO-TEZ-01 | PARTIAL | Hive-on-Tez completed, but the Tez runtime archive was distributed through HDFS rather than CephFS |
| SOAK-04 | FAIL | Two dead-PID sessions remained above the pre-job service baseline after a successful run |
| SOAK-05 | FAIL | Failed applications and log retrieval left sessions that delayed a later native `lstat` call |

## Findings

The Tez AM required the connector and Ceph binding on its additional classpath. Native path injection
had to occur once at the AM layer; duplicate injection created an invalid colon-joined file path.
Successful SQL results did not satisfy the session lifecycle gate.

## Limitations

The run did not cover Tez recovery, scale, Kerberos, or a 24-hour NodeManager lifecycle. Exact failed
application commands are not retained.

## Follow-up

Investigate filesystem close ownership and rerun SOAK-04/SOAK-05 without manual session cleanup.
